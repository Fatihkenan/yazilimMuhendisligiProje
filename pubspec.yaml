name: yazilimmuhendislgiproje
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  shimmer: ^3.0.0

  cupertino_icons: ^1.0.8

  firebase_core: ^3.1.1
  firebase_auth: ^5.1.2
  cloud_firestore: ^5.0.2

  firebase_storage: ^12.1.0
  image_picker: ^1.1.2
  file_picker: ^8.0.3
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: trueئ
